#include "DiveComp.h"
#include "DivingWatches.h"
#include "GTMWatches.h"
#include "Temp.h"
#include "User.h"
#include <string>

#pragma once

std::string path;

void SetPath(std::string new_path) {
	if(fopen(new_path))
}
